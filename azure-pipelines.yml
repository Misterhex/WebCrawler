
trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

steps:

- task: DotNetCoreCLI@2
  inputs:
    command: 'pack'
    packagesToPack: '**/WebCrawling.csproj'
    includesymbols: true
    versioningScheme: 'byBuildNumber'
    verbosityPack: 'Normal'